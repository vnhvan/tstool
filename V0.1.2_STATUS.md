# Township Offline Save Editor v0.1.2

## Hoàn thành

- Tự động tạo backup trước khi tạo save đã chỉnh.
- Backup được ghi qua file tạm, kiểm tra kích thước và SHA-256 rồi mới đổi tên.
- Không tạo bản sao trùng SHA-256; dùng lại backup giống hệt đã có.
- Giữ tối đa 25 backup gần nhất.
- Khi đọc backup, kiểm tra lại file không rỗng và SHA-256.
- XML có BOM/khoảng trắng đầu file được nhận diện đúng ở chế độ chỉ đọc.
- Đổi nhãn “Mở/khôi phục” thành “Mở backup” để không gây hiểu nhầm: v0.1.2 chưa ghi trực tiếp vào thư mục game.

## Giới hạn

- Chưa build APK trong môi trường hiện tại vì thiếu Android SDK/dependency cache.
- Chưa ghi trực tiếp vào `/data/data/com.playrix.township.vn/saves`.
- Module ghi đã xác minh hiện mới có Sound Volume.
