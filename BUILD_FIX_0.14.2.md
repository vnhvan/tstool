# Build fix 0.14.2

Fixed:
- Material3 experimental API opt-in for CenterAlignedTopAppBar.
- KeyboardOptions import moved to androidx.compose.foundation.text.KeyboardOptions.
- SafeEditEngineTest now uses JUnit 4.13 assertThrows instead of the nonexistent org.junit.Assert.assertFailsWith.
