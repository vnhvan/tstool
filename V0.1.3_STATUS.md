# Township Offline Save Editor v0.1.3

## ĐÃ XÁC MINH
- Phân loại XML, container 0x79 và file không xác định trước khi giải mã.
- Phát hiện XML thiếu `</root>`, header 0x79 bị cắt và payload khai báo vượt dữ liệu thực.
- Backup có báo cáo kiểm tra SHA-256 theo tên file và cấu trúc container.
- Chỉ cho mở/xuất lại backup vượt qua kiểm tra tính toàn vẹn.
- Giao diện có nút Kiểm tra và Xuất lại cho từng backup.

## ĐANG LÀM
- Build APK debug bằng Android SDK/Android Studio.
- Kiểm thử UI thật trên LDPlayer.

## CHƯA XÁC MINH
- Ghi trực tiếp vào thư mục save bằng root.
- Khôi phục nguyên tử vào thư mục game.
- Coin, TCash và các module chưa có bộ mẫu cô lập.

## PHÁT HIỆN MỚI
- `renameTo()` không bảo đảm thành công trên mọi filesystem; đã thêm fallback copy có xác minh.
- Trước v0.1.3, nút mở backup kiểm SHA nhưng không hiển thị được nguyên nhân lỗi chi tiết.
